1,组合模式不是父子关系，虽然说的是父子节点，但实际上是聚合（HAS-A）；组合对象把请求委托给它所包含的所有叶对象，
  成功的关键是它们都有同样的接口（execute，scan方法）
2，除了相同的接口，还要有一致性
3，一一对应，双向映射，一个叶节点不能对应多个父节点

何时使用：
1，不清楚会有多少层，但是部分-整体层次清晰。符合开放-封闭原则：修改树的结构，但是不用修改代码
2，组合对象和叶对象可以统一看待